# 🤖 Llama AI Agent 工具增強計畫

> **建立日期：** 2026-01-09  
> **目標：** 讓 Llama 聊天機器人具備執行系統命令和工具調用的能力  
> **狀態：** 📋 計畫階段

---

## 📋 目錄

1. [問題背景](#1-問題背景)
2. [目標與限制](#2-目標與限制)
3. [架構設計](#3-架構設計)
4. [安全機制](#4-安全機制)
5. [實作計畫](#5-實作計畫)
6. [使用範例](#6-使用範例)
7. [測試計畫](#7-測試計畫)
8. [未來擴展](#8-未來擴展)

---

## 1. 問題背景

### 1.1 當前狀況

**現有的 Llama 聊天機器人：**
- ✅ 可以理解和生成文本
- ✅ 可以回答問題
- ❌ **無法執行系統命令**（如 `.bat`、`.ps1`、`.exe`）
- ❌ **無法訪問文件系統**
- ❌ **無法運行程式碼**

**用戶需求：**
```
User: 你能執行 C:\Users\svd\codes\openvino-lab\examples\run_llama_chatbot.bat 這個程式嗎？
Llama: ？ (只能生成文本，無法執行)
```

### 1.2 LLM 的本質限制

大型語言模型（LLM）如 Llama：
- **只是文本生成器**，不是程式執行器
- **沒有外部工具調用能力**（Tool Calling / Function Calling）
- **需要額外的框架**來連接外部世界

### 1.3 解決方案概述

建立一個 **AI Agent 系統**：
```
User Input → Llama (理解意圖) → Tool Executor (執行命令) → Llama (整理結果) → User
```

---

## 2. 目標與限制

### 2.1 專案目標

建立 `examples/llama_agent.py`，具備以下能力：

#### 核心功能
1. **系統命令執行** 🔧
   - 執行 `.bat` 檔案
   - 執行 `.ps1` 腳本
   - 執行 `.exe` 程式
   - 執行 PowerShell 命令

2. **文件系統操作** 📁
   - 讀取檔案內容
   - 寫入檔案
   - 列出目錄內容
   - 檢查檔案存在

3. **Python 程式碼執行** 🐍
   - 執行簡單的 Python 程式碼
   - 查看執行結果

4. **對話記憶** 🧠
   - 記住之前的對話
   - 理解上下文

### 2.2 安全限制

**必須實作的安全機制：**

1. **路徑限制** 🔒
   - 只能在專案目錄內操作（`C:\Users\svd\codes\openvino-lab\`）
   - 禁止訪問系統目錄（如 `C:\Windows\`、`C:\Program Files\`）
   - 禁止訪問敏感檔案（如 `.ssh\`、`.aws\`）

2. **命令白名單** ✅
   - 只允許安全的命令
   - 禁止危險命令（如 `format`、`del /s`、`rm -rf`）

3. **執行確認** ⚠️
   - 執行前顯示要執行的命令
   - 需要用戶確認（可選關閉）

4. **輸出限制** 📊
   - 限制輸出長度（避免佔用過多記憶體）
   - 超時保護（避免無限循環）

5. **日誌記錄** 📝
   - 記錄所有執行的命令
   - 記錄執行結果和錯誤

### 2.3 非目標

**不在此階段實作：**
- ❌ 網絡訪問（HTTP 請求）
- ❌ 數據庫操作
- ❌ 多使用者系統
- ❌ 遠程命令執行
- ❌ 自動化部署

---

## 3. 架構設計

### 3.1 系統架構圖

```
┌─────────────────────────────────────────────────────────────┐
│                      User Interface                          │
│                    (Terminal / Chat)                         │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                   Llama Agent Core                           │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  1. Intent Recognition (意圖識別)                      │  │
│  │     - 使用者想做什麼？                                  │  │
│  │     - 需要調用哪個工具？                                │  │
│  └───────────────────────────────────────────────────────┘  │
│                       │                                       │
│                       ▼                                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  2. Tool Router (工具路由)                             │  │
│  │     - 選擇合適的工具                                    │  │
│  │     - 準備參數                                         │  │
│  └───────────────────────────────────────────────────────┘  │
│                       │                                       │
│                       ▼                                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  3. Safety Checker (安全檢查)                          │  │
│  │     - 路徑檢查                                         │  │
│  │     - 命令檢查                                         │  │
│  │     - 權限檢查                                         │  │
│  └───────────────────────────────────────────────────────┘  │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                   Tool Executors                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Command    │  │     File     │  │    Python    │      │
│  │   Executor   │  │   Operator   │  │   Executor   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                  Operating System                            │
│            (Windows / PowerShell / File System)              │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 核心組件

#### 3.2.1 Intent Recognition（意圖識別）

使用 Llama 模型識別用戶意圖：

```python
class IntentRecognizer:
    """識別用戶意圖並提取參數"""
    
    def recognize(self, user_input: str) -> Dict:
        """
        輸入：你能執行 C:\Users\svd\codes\openvino-lab\examples\run_llama_chatbot.bat 這個程式嗎
        
        輸出：
        {
            "intent": "execute_command",
            "tool": "command_executor",
            "params": {
                "command": "C:\\Users\\svd\\codes\\openvino-lab\\examples\\run_llama_chatbot.bat"
            }
        }
        """
```

**支援的意圖類型：**
- `execute_command` - 執行命令
- `read_file` - 讀取檔案
- `write_file` - 寫入檔案
- `list_directory` - 列出目錄
- `execute_python` - 執行 Python 程式碼
- `chat` - 普通對話（不需要工具）

#### 3.2.2 Tool Router（工具路由）

```python
class ToolRouter:
    """根據意圖選擇合適的工具"""
    
    def __init__(self):
        self.tools = {
            "execute_command": CommandExecutor(),
            "read_file": FileOperator().read,
            "write_file": FileOperator().write,
            "list_directory": FileOperator().list_dir,
            "execute_python": PythonExecutor(),
        }
    
    def route(self, intent: Dict) -> Callable:
        """返回對應的工具函數"""
        return self.tools.get(intent["tool"])
```

#### 3.2.3 Safety Checker（安全檢查）

```python
class SafetyChecker:
    """安全檢查器"""
    
    def __init__(self, project_root: str):
        self.project_root = os.path.abspath(project_root)
        self.forbidden_commands = [
            "format", "del /s", "rm -rf", "mkfs",
            "shutdown", "restart", "reboot"
        ]
        self.forbidden_paths = [
            "C:\\Windows", "C:\\Program Files",
            "C:\\Users\\*\\.ssh", "C:\\Users\\*\\.aws"
        ]
    
    def check_path(self, path: str) -> Tuple[bool, str]:
        """檢查路徑是否安全"""
        abs_path = os.path.abspath(path)
        
        # 必須在專案目錄內
        if not abs_path.startswith(self.project_root):
            return False, f"路徑必須在專案目錄內: {self.project_root}"
        
        # 檢查禁止訪問的路徑
        for forbidden in self.forbidden_paths:
            if fnmatch.fnmatch(abs_path, forbidden):
                return False, f"禁止訪問敏感路徑: {forbidden}"
        
        return True, "OK"
    
    def check_command(self, command: str) -> Tuple[bool, str]:
        """檢查命令是否安全"""
        cmd_lower = command.lower()
        
        for forbidden in self.forbidden_commands:
            if forbidden in cmd_lower:
                return False, f"禁止執行危險命令: {forbidden}"
        
        return True, "OK"
```

#### 3.2.4 Tool Executors（工具執行器）

##### Command Executor（命令執行器）

```python
class CommandExecutor:
    """執行系統命令"""
    
    def execute(self, command: str, timeout: int = 30) -> Dict:
        """
        執行命令並返回結果
        
        Args:
            command: 要執行的命令
            timeout: 超時時間（秒）
        
        Returns:
            {
                "success": True/False,
                "stdout": "標準輸出",
                "stderr": "錯誤輸出",
                "return_code": 0
            }
        """
        try:
            result = subprocess.run(
                command,
                shell=True,
                capture_output=True,
                text=True,
                timeout=timeout,
                cwd=self.project_root
            )
            
            return {
                "success": result.returncode == 0,
                "stdout": result.stdout[:5000],  # 限制輸出長度
                "stderr": result.stderr[:5000],
                "return_code": result.returncode
            }
        except subprocess.TimeoutExpired:
            return {
                "success": False,
                "error": f"命令執行超時（>{timeout}秒）"
            }
        except Exception as e:
            return {
                "success": False,
                "error": str(e)
            }
```

##### File Operator（檔案操作器）

```python
class FileOperator:
    """檔案系統操作"""
    
    def read(self, path: str) -> Dict:
        """讀取檔案"""
        try:
            with open(path, 'r', encoding='utf-8') as f:
                content = f.read(10000)  # 限制讀取大小
            return {"success": True, "content": content}
        except Exception as e:
            return {"success": False, "error": str(e)}
    
    def write(self, path: str, content: str) -> Dict:
        """寫入檔案"""
        try:
            with open(path, 'w', encoding='utf-8') as f:
                f.write(content)
            return {"success": True, "message": f"已寫入 {len(content)} 字元"}
        except Exception as e:
            return {"success": False, "error": str(e)}
    
    def list_dir(self, path: str) -> Dict:
        """列出目錄內容"""
        try:
            items = os.listdir(path)
            return {"success": True, "items": items[:100]}  # 限制數量
        except Exception as e:
            return {"success": False, "error": str(e)}
```

##### Python Executor（Python 執行器）

```python
class PythonExecutor:
    """執行 Python 程式碼（沙盒模式）"""
    
    def execute(self, code: str, timeout: int = 10) -> Dict:
        """
        在受限環境中執行 Python 程式碼
        
        限制：
        - 不能 import 危險模組（os, sys, subprocess 等）
        - 執行時間限制
        - 記憶體限制
        """
        # TODO: 實作沙盒執行
        # 可使用 RestrictedPython 或自訂限制
        pass
```

### 3.3 工作流程

```
1. User: "執行 examples\run_llama_chatbot.bat"
   │
   ▼
2. Intent Recognition (Llama 分析)
   → intent: "execute_command"
   → params: {"command": "examples\\run_llama_chatbot.bat"}
   │
   ▼
3. Safety Check
   → 路徑檢查: ✅ 在專案目錄內
   → 命令檢查: ✅ 安全
   │
   ▼
4. User Confirmation (可選)
   → 顯示: "即將執行: examples\run_llama_chatbot.bat"
   → 等待確認: Y/n
   │
   ▼
5. Command Execution
   → subprocess.run(...)
   → 捕獲輸出
   │
   ▼
6. Result Processing (Llama 整理)
   → 將執行結果轉換為自然語言
   │
   ▼
7. Response to User
   → "✅ 命令執行成功！輸出: ..."
```

---

## 4. 安全機制

### 4.1 多層防護

```
Layer 1: 路徑檢查
  └→ 只允許專案目錄內的操作

Layer 2: 命令白名單/黑名單
  └→ 禁止危險命令

Layer 3: 執行前確認
  └→ 顯示並等待用戶確認

Layer 4: 超時保護
  └→ 避免無限循環

Layer 5: 輸出限制
  └→ 限制記憶體使用

Layer 6: 日誌記錄
  └→ 可追蹤所有操作
```

### 4.2 配置文件

`config/agent_config.yaml`:
```yaml
# AI Agent 安全配置

security:
  # 專案根目錄
  project_root: "C:\\Users\\svd\\codes\\openvino-lab"
  
  # 是否需要執行前確認
  require_confirmation: true
  
  # 禁止的命令關鍵字
  forbidden_commands:
    - "format"
    - "del /s"
    - "rm -rf"
    - "shutdown"
    - "restart"
  
  # 禁止訪問的路徑模式
  forbidden_paths:
    - "C:\\Windows\\*"
    - "C:\\Program Files\\*"
    - "*\\.ssh\\*"
    - "*\\.aws\\*"
  
  # 執行超時（秒）
  timeout:
    command: 30
    python: 10
  
  # 輸出限制（字元數）
  max_output_length: 5000

# 日誌配置
logging:
  enabled: true
  log_file: "config/logs/agent_log.txt"
  log_level: "INFO"
```

### 4.3 風險評估

| 風險 | 嚴重性 | 緩解措施 | 狀態 |
|------|--------|----------|------|
| 惡意命令執行 | 🔴 高 | 命令黑名單、確認機制 | ✅ 已實作 |
| 敏感檔案訪問 | 🔴 高 | 路徑白名單、禁止列表 | ✅ 已實作 |
| 無限循環/當機 | 🟡 中 | 超時保護 | ✅ 已實作 |
| 記憶體耗盡 | 🟡 中 | 輸出限制 | ✅ 已實作 |
| 意圖識別錯誤 | 🟡 中 | 執行前確認 | ✅ 已實作 |
| 日誌洩漏 | 🟢 低 | 日誌權限控制 | ⏳ 待實作 |

---

## 5. 實作計畫

### 5.1 開發階段

#### Phase 1: 核心框架 (第 1-2 天)
- [ ] 建立 `examples/llama_agent.py` 主程式
- [ ] 實作 `IntentRecognizer` 類別
- [ ] 實作 `SafetyChecker` 類別
- [ ] 實作 `ToolRouter` 類別
- [ ] 建立配置文件 `config/agent_config.yaml`

#### Phase 2: 工具執行器 (第 3-4 天)
- [ ] 實作 `CommandExecutor` - 命令執行
- [ ] 實作 `FileOperator` - 檔案操作
- [ ] 實作基本的日誌系統
- [ ] 單元測試（安全性測試）

#### Phase 3: 對話整合 (第 5 天)
- [ ] 整合 Llama 模型
- [ ] 實作對話循環
- [ ] 實作執行前確認機制
- [ ] 美化輸出格式

#### Phase 4: 進階功能 (第 6-7 天)
- [ ] 實作 `PythonExecutor` (可選)
- [ ] 實作對話歷史記憶
- [ ] 實作多輪對話上下文
- [ ] 完整的錯誤處理

#### Phase 5: 測試與文檔 (第 8-9 天)
- [ ] 完整的功能測試
- [ ] 安全性測試
- [ ] 編寫使用文檔
- [ ] 編寫範例

#### Phase 6: 優化與發布 (第 10 天)
- [ ] 效能優化
- [ ] 使用者體驗優化
- [ ] 最終測試
- [ ] 發布 v1.0

### 5.2 檔案結構

```
openvino-lab/
├── examples/
│   ├── llama_agent.py              ← 主程式
│   ├── agent/                      ← Agent 模組
│   │   ├── __init__.py
│   │   ├── intent_recognizer.py   ← 意圖識別
│   │   ├── safety_checker.py      ← 安全檢查
│   │   ├── tool_router.py         ← 工具路由
│   │   └── executors/             ← 執行器
│   │       ├── __init__.py
│   │       ├── command.py         ← 命令執行器
│   │       ├── file.py            ← 檔案操作器
│   │       └── python.py          ← Python 執行器
│   └── tests/
│       └── test_agent.py          ← 測試
├── config/
│   ├── agent_config.yaml          ← Agent 配置
│   └── logs/
│       └── agent_log.txt          ← 執行日誌
└── docs/
    └── llama/
        ├── AI_AGENT_PLAN.md       ← 本文件
        ├── AI_AGENT_USAGE.md      ← 使用指南（待建立）
        └── AI_AGENT_API.md        ← API 文檔（待建立）
```

---

## 6. 使用範例

### 6.1 基本使用

```powershell
# 啟動 AI Agent
.\venv\Scripts\python.exe examples\llama_agent.py

# 或指定設備
.\venv\Scripts\python.exe examples\llama_agent.py --device GPU
```

### 6.2 對話範例

#### 範例 1: 執行命令

```
You: 請執行 examples\run_llama_chatbot.bat

Agent: 我了解您想執行一個批次檔。讓我先檢查一下...

[安全檢查]
✅ 路徑檢查通過: 在專案目錄內
✅ 命令檢查通過: 安全

[執行確認]
即將執行命令:
  examples\run_llama_chatbot.bat

是否繼續？ (Y/n): Y

[執行中...]
⏳ 正在執行命令...

[執行結果]
✅ 命令執行成功！

輸出:
=== Llama Chatbot - Quick Run Hint ===

1) Activate the virtual environment (PowerShell):
   .\venv\Scripts\Activate.ps1
...

是否還有其他需要幫助的？
```

#### 範例 2: 讀取檔案

```
You: 幫我看一下 README.md 的內容

Agent: 好的，我來讀取 README.md 檔案...

[安全檢查]
✅ 路徑檢查通過: README.md

[讀取中...]
⏳ 正在讀取檔案...

[檔案內容]
# OpenVINO GenAI Lab

這是一個用於學習和實驗 OpenVINO GenAI 的專案環境。
...

檔案總共 1523 字元。是否需要我解釋特定部分？
```

#### 範例 3: 列出目錄

```
You: examples 資料夾裡有什麼檔案？

Agent: 讓我為您列出 examples 資料夾的內容...

[examples/ 目錄內容]
📁 agent/
📄 check_llama_env.py
📄 llama_agent.py
📄 llama_batch_inference.py
📄 llama_chatbot.py
📄 llama_quick_start.py
📄 run_llama_chatbot.bat
📄 simple_inference.py
📁 tests/

共 5 個檔案，3 個資料夾。
```

#### 範例 4: 寫入檔案

```
You: 幫我建立一個 test.txt 檔案，內容是 "Hello, World!"

Agent: 我將為您建立 test.txt 檔案...

[安全檢查]
✅ 路徑檢查通過: test.txt (在專案目錄內)

[執行確認]
即將執行操作:
  寫入檔案: test.txt
  內容長度: 13 字元

是否繼續？ (Y/n): Y

[寫入中...]
⏳ 正在寫入檔案...

[完成]
✅ 檔案建立成功！
   路徑: C:\Users\svd\codes\openvino-lab\test.txt
   大小: 13 字元
```

### 6.3 錯誤處理範例

#### 範例 1: 禁止的路徑

```
You: 幫我讀取 C:\Windows\System32\config\SAM

Agent: ⚠️ 安全檢查失敗！

原因: 禁止訪問敏感路徑
路徑: C:\Windows\System32\*

為了您的系統安全，我只能訪問專案目錄內的檔案:
C:\Users\svd\codes\openvino-lab\

請問還有其他我能幫忙的嗎？
```

#### 範例 2: 危險命令

```
You: 執行 format C:

Agent: 🛑 安全檢查失敗！

原因: 禁止執行危險命令
命令關鍵字: format

此命令可能會造成系統損壞，因此被阻止執行。

如果您真的需要執行這類命令，請直接在終端機中操作。
```

---

## 7. 測試計畫

### 7.1 功能測試

#### 測試案例 1: 命令執行
```python
def test_execute_command():
    """測試命令執行功能"""
    agent = LlamaAgent()
    
    # 測試 1: 執行簡單命令
    result = agent.process("執行 echo Hello")
    assert result["success"] == True
    assert "Hello" in result["output"]
    
    # 測試 2: 執行 .bat 檔案
    result = agent.process("執行 examples\\run_llama_chatbot.bat")
    assert result["success"] == True
    
    # 測試 3: 執行不存在的命令
    result = agent.process("執行 non_existent_command")
    assert result["success"] == False
```

#### 測試案例 2: 檔案操作
```python
def test_file_operations():
    """測試檔案操作功能"""
    agent = LlamaAgent()
    
    # 測試 1: 讀取檔案
    result = agent.process("讀取 README.md")
    assert result["success"] == True
    assert len(result["content"]) > 0
    
    # 測試 2: 寫入檔案
    result = agent.process("寫入 test.txt 內容是 Hello")
    assert result["success"] == True
    
    # 測試 3: 列出目錄
    result = agent.process("列出 examples 資料夾")
    assert result["success"] == True
    assert "llama_chatbot.py" in result["items"]
```

### 7.2 安全性測試

#### 測試案例 3: 路徑安全
```python
def test_path_security():
    """測試路徑安全機制"""
    agent = LlamaAgent()
    
    # 測試 1: 禁止訪問系統目錄
    result = agent.process("讀取 C:\\Windows\\System32\\config\\SAM")
    assert result["success"] == False
    assert "禁止訪問" in result["error"]
    
    # 測試 2: 禁止訪問 .ssh 目錄
    result = agent.process("列出 C:\\Users\\user\\.ssh")
    assert result["success"] == False
    
    # 測試 3: 允許專案目錄
    result = agent.process("列出 examples")
    assert result["success"] == True
```

#### 測試案例 4: 命令安全
```python
def test_command_security():
    """測試命令安全機制"""
    agent = LlamaAgent()
    
    # 測試 1: 禁止 format 命令
    result = agent.process("執行 format C:")
    assert result["success"] == False
    assert "危險命令" in result["error"]
    
    # 測試 2: 禁止 rm -rf 命令
    result = agent.process("執行 rm -rf /")
    assert result["success"] == False
    
    # 測試 3: 允許安全命令
    result = agent.process("執行 echo test")
    assert result["success"] == True
```

### 7.3 效能測試

```python
def test_performance():
    """測試效能"""
    agent = LlamaAgent()
    
    # 測試 1: 回應時間
    start = time.time()
    result = agent.process("執行 echo test")
    elapsed = time.time() - start
    assert elapsed < 5.0  # 應在 5 秒內完成
    
    # 測試 2: 超時保護
    result = agent.process("執行一個需要很長時間的命令")
    assert result.get("timeout") == True
```

---

## 8. 未來擴展

### 8.1 短期擴展（1-2 週）

1. **Python 程式碼執行器** 🐍
   - 沙盒環境
   - 限制可用模組
   - 資源限制

2. **更智能的意圖識別** 🧠
   - 多輪對話理解
   - 上下文記憶
   - 模糊匹配

3. **更多工具** 🔧
   - Git 操作（status, diff, commit）
   - 環境變數管理
   - 進程管理

### 8.2 中期擴展（1-2 個月）

1. **網絡功能** 🌐
   - HTTP 請求
   - API 調用
   - 網頁抓取

2. **數據處理** 📊
   - CSV/JSON 處理
   - 數據分析
   - 圖表生成

3. **插件系統** 🔌
   - 自訂工具
   - 社群擴展
   - 工具市場

### 8.3 長期願景（3-6 個月）

1. **分散式執行** 🌍
   - 遠程命令執行
   - 多機器協同
   - 任務調度

2. **自動化工作流** ⚙️
   - 工作流定義
   - 定時任務
   - 事件觸發

3. **GUI 介面** 🖥️
   - Web UI
   - 桌面應用
   - VS Code 擴展

---

## 9. 參考資源

### 9.1 類似專案

- [LangChain](https://github.com/langchain-ai/langchain) - LLM 應用框架
- [AutoGPT](https://github.com/Significant-Gravitas/AutoGPT) - 自主 AI Agent
- [BabyAGI](https://github.com/yoheinakajima/babyagi) - 任務驅動 AI
- [OpenAI Function Calling](https://platform.openai.com/docs/guides/function-calling) - 函數調用 API

### 9.2 技術文檔

- [OpenVINO GenAI](https://openvinotoolkit.github.io/openvino.genai/)
- [Python subprocess](https://docs.python.org/3/library/subprocess.html)
- [RestrictedPython](https://restrictedpython.readthedocs.io/) - Python 沙盒

### 9.3 安全資源

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Command Injection Prevention](https://cheatsheetseries.owasp.org/cheatsheets/OS_Command_Injection_Defense_Cheat_Sheet.html)

---

## 10. 常見問題

### Q1: 為什麼需要 AI Agent？

**A:** Llama 本身只能生成文本，無法執行命令。AI Agent 是一個框架，讓 Llama 可以：
- 理解用戶想執行什麼操作
- 調用相應的工具
- 返回執行結果

### Q2: 這安全嗎？

**A:** 我們實作了多層安全機制：
- ✅ 路徑限制（只能在專案目錄內）
- ✅ 命令黑名單（禁止危險命令）
- ✅ 執行前確認
- ✅ 超時保護
- ✅ 日誌記錄

但仍建議：
- 不要在生產環境使用
- 不要授予過高權限
- 定期檢查日誌

### Q3: 可以執行任何命令嗎？

**A:** 不可以。有以下限制：
- 只能在專案目錄內操作
- 禁止危險命令（format, rm -rf 等）
- 禁止訪問敏感檔案（.ssh, .aws 等）
- 需要執行前確認（可配置）

### Q4: 與 ChatGPT Code Interpreter 有什麼不同？

**A:** 
- **相似處：** 都能執行程式碼和命令
- **不同處：**
  - 我們使用本地 Llama 模型（更私密）
  - 完全在本地執行（更安全）
  - 可以自訂工具和安全規則
  - 專為開發環境設計

### Q5: 效能如何？

**A:** 
- **意圖識別：** ~1-2 秒（取決於 Llama 模型大小）
- **命令執行：** 即時（取決於命令本身）
- **整體回應：** ~2-5 秒

可以透過以下方式優化：
- 使用 GPU 加速
- 使用更小的模型（INT4 量化）
- 快取常見意圖

---

## ✅ 檢查清單

### 開始前
- [ ] 閱讀完整計畫文件
- [ ] 理解架構設計
- [ ] 了解安全機制
- [ ] 準備開發環境

### Phase 1 完成後
- [ ] 核心框架建立完成
- [ ] 配置文件建立完成
- [ ] 安全檢查器測試通過
- [ ] 意圖識別基本可用

### Phase 2 完成後
- [ ] 命令執行器可用
- [ ] 檔案操作器可用
- [ ] 日誌系統運作
- [ ] 單元測試通過

### Phase 3 完成後
- [ ] Llama 整合完成
- [ ] 對話循環運作
- [ ] 確認機制可用
- [ ] 輸出格式美化

### 最終發布前
- [ ] 所有功能測試通過
- [ ] 安全性測試通過
- [ ] 使用文檔完成
- [ ] 範例測試通過

---

**計畫狀態：** 📋 待實作  
**預估工時：** 10 個工作天  
**優先級：** 🔴 高（用戶需求明確）  
**下一步：** 開始 Phase 1 - 核心框架建立

**準備好開始了嗎？** 🚀
