# Git 忽略配置說明

## 📁 模型下載位置

當您運行 `.\scripts\prepare_models.ps1` 時，模型會下載到：

```
📦 c:\Users\svd\codes\openvino-lab\
├── 📁 models/                          ← 模型保存位置
│   ├── 📁 TinyLlama-1.1B-int4/
│   │   ├── config.json
│   │   ├── tokenizer.json
│   │   ├── model.safetensors
│   │   └── ...
│   ├── 📁 Qwen2-1.5B-int4/
│   └── 📁 phi-2-int4/
└── ...
```

## ⚙️ .gitignore 配置

已更新 `.gitignore` 文件以排除以下内容：

### 1️⃣ 模型文件（最重要）

```
models/              # 整個模型目錄
*.model
*.onnx
*.safetensors
*.bin
*.pt
*.pth
```

**為什麼要忽略？**
- 模型文件非常大（通常 500MB - 10GB+）
- 不適合上傳到 Git 倉庫
- 應該在本地或通過 CI/CD 下載

### 2️⃣ 環境變數文件

```
.env                 # 敏感信息和本地配置
config/.env
```

**為什麼要忽略？**
- 包含本地路徑和敏感配置
- 不同開發機器可能需要不同配置

### 3️⃣ 虛擬環境

```
venv/
env/
.venv
```

**為什麼要忽略？**
- 虛擬環境很大，不需要上傳
- 每個開發者運行 `python -m venv venv` 即可重建

### 4️⃣ Python 緩存和編譯文件

```
__pycache__/
*.pyc
*.pyo
*.egg-info/
```

### 5️⃣ IDE 和編輯器配置

```
.vscode/
.idea/
*.swp
```

### 6️⃣ 日誌、數據和輸出

```
logs/
*.log
data/
outputs/
metrics/
```

## 📊 完整 .gitignore 結構

```
# ==================== Python 虛擬環境 ====================
venv/
env/

# ==================== AI 模型和大型文件 ====================
models/              # ← 關鍵：模型目錄
*.safetensors
*.onnx
*.bin
*.pt

# ==================== 環境配置 ====================
.env                 # ← 關鍵：本地環境變數
config/.env

# ==================== 其他 ====================
__pycache__/
.vscode/
logs/
...
```

## ✅ 驗證配置

### 檢查忽略規則是否生效

```powershell
# 查看哪些文件會被忽略
git check-ignore -v models/
git check-ignore -v config/.env
git check-ignore -v venv/
```

**預期輸出：**
```
models/        .gitignore:22:models/
config/.env    .gitignore:31:config/.env
venv/          .gitignore:1:venv/
```

### 查看 Git 追蹤的文件

```powershell
# 只查看會被提交的文件
git ls-files

# 驗證模型目錄被忽略
git ls-files | findstr models
# （應該沒有輸出）
```

## 🚀 推薦的項目工作流程

### 1️⃣ 初次設置

```powershell
# 克隆倉庫
git clone <repo-url>
cd openvino-lab

# 創建虛擬環境（被忽略，本地生成）
python -m venv venv
.\venv\Scripts\Activate.ps1

# 安裝依賴
pip install -r requirements.txt

# 複製環境配置（被忽略，本地配置）
Copy-Item config\.env.example config\.env

# 下載模型（被忽略，本地生成）
.\scripts\prepare_models.ps1
```

### 2️⃣ 開發和提交

```powershell
# 檢查狀態
git status
# 應該看不到：models/, venv/, config/.env

# 提交只會包含：
# - 源代碼
# - 配置模板 (config/.env.example)
# - 文檔和腳本
```

### 3️⃣ 隊友協作

```powershell
# 隊友拉取代碼後
git pull

# 他們需要自己運行：
python -m venv venv
pip install -r requirements.txt
.\scripts\prepare_models.ps1
```

## 📋 文件大小對比

| 項目 | 大小 | 上傳？ |
|------|------|--------|
| 源代碼 | ~50 KB | ✅ 是 |
| requirements.txt | ~2 KB | ✅ 是 |
| config/.env.example | ~2 KB | ✅ 是 |
| TinyLlama 模型 | ~600 MB | ❌ 否 |
| venv 虛擬環境 | ~500 MB | ❌ 否 |
| config/.env | ~2 KB | ❌ 否 |

**總計上傳大小：** ~54 KB（而不是 1+ GB）

## 🎯 何時修改 .gitignore

### 添加新的忽略規則

```bash
# 編輯 .gitignore
notepad .gitignore

# 添加新規則，例如：
experiment_results/
*.tmp

# 提交更改
git add .gitignore
git commit -m "Add new ignore rules for experiment results"
```

### 不小心提交了大文件怎麼辦？

```powershell
# 從 Git 歷史中移除（但保留本地文件）
git rm --cached models/
git commit -m "Remove models from tracking"

# 確保 .gitignore 包含 models/
# 下次就不會再上傳了
```

## 💡 最佳實踐

1. ✅ **定期檢查** `git status` 確保沒有意外的大文件
2. ✅ **使用 `.env.example`** 作為配置模板，簽入倉庫
3. ✅ **在 README 中說明** 如何下載模型
4. ✅ **使用 CI/CD** 自動下載模型（如果需要自動化）
5. ❌ **不要簽入** 模型、虛擬環境或本地配置

## 🔗 相關文檔

- [.gitignore 配置文件](../../.gitignore)
- [快速開始指南](README.md)
- [Stage 7 模型準備](STAGE_7_GUIDE.md)

---

**更新日期：** 2025年12月30日
